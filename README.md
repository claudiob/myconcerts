Tests for [rspec-api](http://github.com/rspec-api/rspec-api). Run:

* `rspec spec/features/gigs_spec.rb` to test a local Rails app

* `rspec spec/features/github` to test the (remote) [Github API](http://developer.github.com)